name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build and release
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Build archive
        run: |
          xcodebuild archive \
            -scheme window-memory \
            -configuration Release \
            -archivePath build/window-memory.xcarchive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Export app
        run: |
          xcodebuild -exportArchive \
            -archivePath build/window-memory.xcarchive \
            -exportOptionsPlist ExportOptions.plist \
            -exportPath build/export

      - name: Create DMG
        run: |
          hdiutil create \
            -volname "window-memory" \
            -srcfolder build/export/window-memory.app \
            -ov -format UDZO \
            build/window-memory.dmg

      - name: Upload release
        uses: softprops/action-gh-release@v2
        with:
          files: build/window-memory.dmg
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
